from typing import List, Tuple
from parsers.parser import GraphParser

class Partitioner:
    def partition(self, graph_parser: GraphParser, n_partitions: int) -> List[List[Tuple[float]]]:
        # TODO: docstring, partitions: [[(x_min_ex, x_max_inc), (y_min_ex, y_max_inc)], ...]
        pass

apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  replicas: 4
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution: # the whole point is to have workers on different nodes, but allow it
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - worker
                topologyKey: kubernetes.io/hostname
      containers:
        - name: worker
          image: "wrzontek/shortest_path_queries_worker:latest"
          ports:
            - containerPort: 50000
          env:
            - name: PARTITIONER_IP
              value: http://partitioner:49998
            - name: RUST_LOG
              value: "worker=debug"
